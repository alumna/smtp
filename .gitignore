/docs/
/lib/
/bin/
/.shards/
*.dwarf

# Libraries don't need dependency lock
# Dependencies will be locked in applications that use them
/shard.lock

# Local unpublished backend (CODING_AND_TEXT_RULES.md)
shard.override.yml

# Coverage, generated spec binary, local smoke
/coverage/
all_specs.cr
tmp_smoke.cr

# Junit files
/junit/
